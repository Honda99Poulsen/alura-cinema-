import styles from './Footer.module.css'

function Footer(){
    return (
        <footer className={styles.footer}>
            <span>Desarrollado por Alura</span>
        </footer>
    )
}

export default Footer